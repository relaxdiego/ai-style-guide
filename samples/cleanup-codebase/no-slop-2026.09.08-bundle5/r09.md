The working directory `/tmp/tmp.uTeDBxOXj3` is empty except for Claude config, and it's not a git repo, so I have nothing to inspect. Point me at the project or paste the failure and I can actually diagnose it.

Most useful, in rough order:

- The deploy error output, in full, not summarized. The actual stack trace or exit code is usually the whole answer.
- The path to the repo, so I can read the deploy config and recent commits.
- What "deploy" runs here: GitHub Actions, a Dockerfile push, Vercel, a shell script, something else.

"Started failing yesterday" with no code change on your side points at a moving dependency: an unpinned base image, a `latest` tag, a floating action version, an expired token or certificate, or a provider-side change. Which of those it is falls out of the error text immediately.
