I can't see anything to diagnose: `/tmp/tmp.uTeDBxOXj3` is empty apart from `.claude/`, and it isn't a git repo. Point me at the project directory, or paste the failure.

The fastest path is the failing job's log, specifically the first error rather than the final summary line, plus what changed since the last green deploy: your commits, a dependency lockfile update, a base image tag that moves (`:latest`, `:3-slim`), an expired credential or cert, or a change on the deploy target itself. "Started failing yesterday with no code change" usually lands on one of the last three.
