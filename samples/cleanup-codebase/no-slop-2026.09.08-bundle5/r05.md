I need more to go on. What's failing and where?

- The error output from the failing deploy, in full.
- Which deploy system (GitHub Actions, CI script, `kubectl`, Terraform, something else) and whether the repo is on this machine. The working directory `/tmp/tmp.uTeDBxOXj3` isn't a git repo, so point me at the actual path.
- What changed around yesterday: a commit, a dependency bump, a credential rotation, or nothing you know of.

If the logs are on a file here or you can paste them, that's the fastest path.
