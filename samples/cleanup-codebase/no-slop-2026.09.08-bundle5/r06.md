I need to see the failure before I can diagnose it. Two things would move this fastest: the deploy log or error output, and what changed (a commit, a dependency bump, a base image, an expiring credential).

Meanwhile, tell me what deploys this: the working directory `/tmp/tmp.uTeDBxOXj3` isn't a git repository and I haven't looked at it yet, so I don't know your stack or CI system. If the project lives elsewhere, point me at the path and I'll read the config and recent changes myself.
