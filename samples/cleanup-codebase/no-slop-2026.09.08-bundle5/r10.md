I need more to go on. Right now I don't know your stack, your CI system, or what the failure looks like.

Point me at the repo (I'm in `/tmp/tmp.uTeDBxOXj3`, which isn't a git repo), or paste the deploy log with the error. Most useful: the failing step's output, plus what changed yesterday (a merge, a dependency bump, a rotated credential, a base image that moved).
