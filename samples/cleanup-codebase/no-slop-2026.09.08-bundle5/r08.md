I need more to go on. Right now I have a working directory (`/tmp/tmp.uTeDBxOXj3`) that isn't a git repo, and no deploy logs.

Tell me what the deploy is (CI provider, target platform), and paste the failing output. If the project lives somewhere else on this machine, point me at the path and I'll read the config and logs myself.
