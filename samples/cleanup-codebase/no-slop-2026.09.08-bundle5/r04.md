I need more to go on. Right now I have an empty working directory (`/tmp/tmp.uTeDBxOXj3`, not a git repo), so there's nothing for me to inspect.

Point me at the failing deploy: the error output or log, and where the project lives on disk. If the deploy runs in CI, the job log is the fastest path; if it's a script, the command and its output.
