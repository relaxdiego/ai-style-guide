I need something to look at. Right now I have an empty working directory (`/tmp/tmp.hiOmdLYO9S`, not a git repo), so I can't see your project or its CI config.

Give me any of these and I'll dig in:

- The failing deploy log (the error, not a summary).
- Path to the repo on this machine, if it's somewhere else.
- What deploys it: GitHub Actions, GitLab CI, Vercel, Fly, Docker push, something else.
- What changed around yesterday: a merge, a dependency bump, a rotated token, a base image tag that moved.

If you only have one thing, make it the error output.
