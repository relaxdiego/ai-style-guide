I need specifics before I can do anything useful. Right now I know only that something changed between two days ago and yesterday.

Tell me, or point me at:

- **The failure output.** Full error text from the deploy log, not a summary.
- **What deploys this.** GitHub Actions, GitLab CI, Vercel, Fly, Cloud Build, a script someone runs by hand?
- **Where the repo is.** My working directory is `/tmp/tmp.hiOmdLYO9S` and it isn't a git repo, so I can't see your code from here.
- **What moved.** Last known-good deploy vs. first failing one: commits, dependency bumps, base image tag, expiring credentials, a provider incident.

Paste the log and the repo path and I'll take it from there.
