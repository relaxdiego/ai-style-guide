I need more to go on. What's failing, and where can I look?

- What's the deploy target and tool (GitHub Actions, Vercel, Docker + k8s, something else)?
- The actual error output from the failed run.
- What changed around yesterday: a commit, a dependency bump, a rotated credential, a base image tag?

The working directory `/tmp/tmp.hiOmdLYO9S` isn't a git repo, so I have no code or CI config to inspect yet. Point me at the repo path or paste the failure log and I'll dig in.
