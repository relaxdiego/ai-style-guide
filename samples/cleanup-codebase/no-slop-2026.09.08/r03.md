I need more to go on. What's in the working directory, and what's the actual failure output?

Specifically:
- Where does the deploy run (GitHub Actions, GitLab CI, Vercel, a script you run locally)?
- The error text from the failing run, pasted verbatim.
- Did anything change on your side yesterday (a commit, a dependency bump, a rotated credential), or did an untouched pipeline start failing?

Or point me at the repo path and the CI config and I'll dig in myself. The directory I'm in (`/tmp/tmp.hiOmdLYO9S`) isn't a git repo, so I'm guessing your project lives elsewhere.
