I need more to go on. What's in `/tmp/tmp.hiOmdLYO9S`, and what does the failure look like?

Useful specifics:
- The deploy command or CI job, and the error output (full text, not a summary).
- What changed yesterday: commit, dependency bump, base image, credentials rotating, cloud provider change.
- Where it fails: build, push, migration, health check.

Point me at the repo or paste the log and I'll dig in.
