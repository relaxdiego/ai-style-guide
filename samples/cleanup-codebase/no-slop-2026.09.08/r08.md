I need more to go on. Right now I don't know what your deploy is, what changed, or what the failure looks like.

Fastest path: paste the failing output (build log, CI job, or deploy tool error), and tell me where the project lives so I can look. The working directory here (`/tmp/tmp.hiOmdLYO9S`) isn't a git repo and looks empty, so I have nothing to inspect yet.

Also useful if you have it:
- Deploy target and tool (Vercel, Fly, ECS, k8s, GitHub Actions, etc.)
- Whether the same commit deployed fine two days ago
- Anything that changed yesterday on your side: merged PRs, dependency bumps, rotated credentials, changed env vars
