I need more to go on. Right now I don't know what your deploy is, where it runs, or what it says when it fails.

Fastest path: paste the failing deploy log (the error lines, plus ~20 lines before them), and tell me what changed yesterday if anything. Also useful: what the deploy target is (Vercel, ECS, k8s, bare box) and where the repo lives, since `/tmp/tmp.hiOmdLYO9S` isn't a git checkout and I see nothing here to inspect.
