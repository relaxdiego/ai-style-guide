I need more to go on. What's in the working directory, and what does the failure look like?

Specifically:
- The deploy command and its error output (full text, not a summary)
- Where it runs: CI (which system?) or local
- What changed yesterday: commits, dependency bumps, credentials rotating, cloud provider changes

I can start digging if you point me at the repo or logs. `/tmp/tmp.hiOmdLYO9S` isn't a git repository, so I don't have the project yet.
