The working directory `/tmp/tmp.q3pZrhCQiS` is empty except for Claude config, so there's nothing here for me to inspect. I need you to point me at the actual project.

Most useful things to give me:

- **Path to the repo** — or `cd` there and re-launch me.
- **The failure output** — the last ~50 lines of the failing deploy log, verbatim. The error text usually settles it in one read.
- **Where it runs** — GitHub Actions, GitLab CI, Vercel, a shell script, Kubernetes, something else.
- **What changed around yesterday** — your commits, a dependency bump, a base image tag, an expired credential, or nothing at all on your side.

That last one is where I'd start guessing if you want a head start: a deploy that breaks with no commit on your side is usually a floating tag (`:latest`, `node:20`, an unpinned action) that moved under you, or a token/certificate that hit its expiry date.
