I need specifics before I can help. Right now I don't know what the deploy is, where it runs, or what the failure looks like.

Tell me, or point me at the files:

- **Where it runs** — GitHub Actions, GitLab CI, Vercel, a shell script, something else.
- **The error** — paste the failing log output, not just the summary line.
- **What changed** — did the failure start after a commit, a dependency bump, a rotated credential, or nothing you touched?
- **Repo path** — the working directory `/tmp/tmp.q3pZrhCQiS` isn't a git repo and I haven't looked inside it yet. If the project is elsewhere, give me the path.
