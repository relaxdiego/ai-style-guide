The working directory `/tmp/tmp.q3pZrhCQiS` is empty except for Claude config, so I have nothing to inspect. I need you to point me at the project or paste the failure.

Fastest path — any one of these gets me started:

- **The error itself.** Deploy log output, especially the first failure, not the last line.
- **Where the code lives.** An absolute path I can `cd` into.
- **What deploys it.** GitHub Actions, Vercel, Fly, Docker build, Terraform, a shell script.

"Yesterday" is the useful clue. Things that break overnight with no commit from you are usually a moving dependency: an unpinned base image or package version, an expired token or certificate, a runner image upgrade, or a provider deprecation that hit its cutoff date. If nothing on your side changed, tell me that explicitly and I will start there.
