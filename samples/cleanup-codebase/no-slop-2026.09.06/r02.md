The working directory has nothing in it but Claude config — no code, no CI files, no logs. I can't diagnose from here.

Point me at the real thing:

- Path to the repo, or `cd` me there.
- Where the deploy runs: GitHub Actions, GitLab CI, Vercel, Fly, ECS, Kubernetes, something else.
- The failure output — paste the log tail, or tell me the command that produces it and I'll run it.
- What changed around yesterday: your commits, a dependency bump, a base image tag, an expired credential or cert.

If you have the log but it's long, the last ~50 lines plus the first error line is usually enough.
