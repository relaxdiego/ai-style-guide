I need specifics before I can do anything useful. Right now I have a working directory that isn't a git repo and no deploy config in front of me.

Tell me, or point me at:

- **The error.** Paste the failing output — build log, CI job output, deploy step stderr. The exact message matters more than the summary.
- **Where it runs.** GitHub Actions, GitLab CI, Vercel, Fly, Heroku, a shell script on a box?
- **What changed.** Last known good deploy vs. first failure: any merges, dependency bumps, secret rotations, base image updates?
- **Where the code lives.** `/tmp/tmp.q3pZrhCQiS` looks empty and isn't a git repo. Give me the real path and I'll read the config myself.

The "started failing yesterday with no changes on our side" pattern usually resolves to one of: an expired token or certificate, a floating tag (`node:latest`, `ubuntu-latest`) that moved, a transitive dependency published a breaking version, or a registry/runner quota. The log will tell us which in one line.
