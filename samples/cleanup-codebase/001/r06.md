"Clean up" covers a dozen different jobs, and doing the wrong one to a whole codebase is expensive to undo. Tell me which of these you want and I'll start:

- **Dead code** — unused files, functions, exports, dependencies.
- **Formatting/lint** — run the existing formatter and fix lint errors, no logic changes.
- **Structure** — move files, split oversized modules, fix import layout.
- **Duplication** — find and merge repeated logic.
- **Types/errors** — tighten types, remove `any`, fix swallowed exceptions.

Two things I need before touching anything: `/tmp/tmp.EXvmI7K0AS` is not a git repository, so there is no undo for edits I make. Let me `git init` and commit the current state first, or point me at the real repo if this is a scratch copy. Also say whether tests exist and how to run them, since without them I can only make changes I can verify by reading.
