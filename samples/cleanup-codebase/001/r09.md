The working directory is empty — no code, no config, no CI files, not a git repo. Nothing here to diagnose against.

To make progress, point me at the actual project or paste the specifics:

- **Path** to the repo, if it's elsewhere on this machine.
- **Deploy failure output** — the error text and the step it died on, not just "failed".
- **Platform** — GitHub Actions, Vercel, Fly, k8s, a shell script, something else.
- **What changed** around yesterday: your commits, a dependency bump, a rotated credential, or a provider-side change you didn't make.

That last one is where "started failing yesterday with no changes on my side" usually lands: an expired token or certificate, a base image or action tag that moved, or a registry/package version that got yanked.
