I need details before I can do anything useful. Right now I have an empty working directory (`/tmp/tmp.q3pZrhCQiS`, not a git repo) and no deploy config to look at.

Tell me:
- What's the deploy target and tool? (GitHub Actions, Vercel, Fly, Docker + k8s, shell script, something else)
- Where does the code live? Point me at the repo path, or `git init` here if it isn't checked out yet.
- The actual error output — full log tail, not the summary line.
- What changed around yesterday: a commit, a dependency bump, a rotated credential, a base image tag that moved.

Paste the failing log and I'll start there.
