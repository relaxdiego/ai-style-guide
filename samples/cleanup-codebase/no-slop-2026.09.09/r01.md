The working directory has no project in it, only Claude config files. So I can't look at anything yet.

Paste the deploy error output and tell me the path to the repo (or `cd` me there). Also useful: what changed around the time it broke, and what the deploy target is (CI provider, container build, host).
