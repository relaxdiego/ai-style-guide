The working directory (`/tmp/tmp.Lc7UrJXM36`) is empty except for Claude config, and it's not a git repo, so I have nothing to inspect. Point me at the actual project directory, or paste the failing build output.

Most useful details:
- The deploy log, especially the first error rather than the last one
- What runs the deploy (GitHub Actions, Vercel, a shell script, something else)
- Anything that changed around yesterday: a merge, a dependency bump, a rotated credential, a base image tag that moves
