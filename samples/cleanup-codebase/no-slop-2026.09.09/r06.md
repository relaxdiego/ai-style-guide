I need to see the failure before I can help. Two things:

1. The deploy log or error output (paste it, or point me at the file).
2. What's in `/tmp/tmp.Lc7UrJXM36` — that directory isn't a git repo, so I don't know if your project lives there or somewhere else.

If you tell me the repo path and how the deploy runs (CI config, a script, `docker build`, etc.), I'll dig in.
