The working directory is empty (only `.claude/` config), and it isn't a git repo, so there's no deploy config or log here for me to read.

To actually diagnose it, I need one of:
- the failing deploy log or error output, pasted or a path to it
- the path to the real project directory (I can `cd` there or you can restart me there)
- what the deploy is: CI provider, target platform, deploy command

If you have the log handy, paste it and I'll work from that.
