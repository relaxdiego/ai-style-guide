The working directory (`/tmp/tmp.Lc7UrJXM36`) has nothing but Claude config in it, and it's not a git repo, so I have no deploy config, logs, or code to look at.

Point me at the project directory, or paste the failing deploy output. Also useful: what changed between the last good deploy and yesterday's first failure (commits, dependency bumps, CI runner image, credentials/tokens expiring), and what platform runs the deploy.
