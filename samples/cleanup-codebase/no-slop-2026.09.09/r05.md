I need to see the failure before I can diagnose it. Two things:

1. The error output from the failing deploy (full text, not a summary).
2. Where the deploy config lives. The working directory `/tmp/tmp.Lc7UrJXM36` isn't a git repo and I haven't looked at it yet, so point me at the repo path or paste the CI config.

If you'd rather I dig, tell me the repo path and I'll read the pipeline definition and recent changes myself.
