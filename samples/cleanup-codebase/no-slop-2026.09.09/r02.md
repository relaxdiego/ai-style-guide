The working directory `/tmp/tmp.Lc7UrJXM36` is empty except for Claude config, and it isn't a git repo, so I have nothing to inspect. Point me at the actual project directory, or paste the details.

What I need:
- The failing deploy log, full error text, not a summary.
- Which platform runs the deploy (GitHub Actions, Vercel, Fly, Docker to a VM, something else).
- What changed around the time it broke: your commits, a dependency bump, a rotated credential, or nothing on your side.

That last one usually decides it. A deploy that breaks with no commits on your side is almost always an expired token, a base image or runner update, or a registry/package version that moved.
