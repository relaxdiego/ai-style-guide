I need to look at the actual failure before guessing. What do you have?

- The error output from the failing deploy (full text, not a summary)
- What deploys this: GitHub Actions, GitLab CI, Vercel, a shell script?
- Whether anything changed around the time it broke: a merge, a dependency bump, a rotated credential, a platform upgrade

The working directory `/tmp/tmp.Lc7UrJXM36` isn't a git repo and I don't know if your project is here. Point me at the repo path and I'll dig through the config and recent commits myself.
