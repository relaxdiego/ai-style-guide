The working directory is empty except Claude config, so there's nothing here for me to inspect. I need you to point me at the actual project or paste the failure.

Most useful things, in order:
- The failing build/deploy log, especially the first error rather than the last.
- Path to the repo, if it's on this machine.
- What deploys it (GitHub Actions, Vercel, Fly, a shell script) and whether anything changed yesterday: a merged PR, a dependency bump, a rotated credential, a base image that moved.

If the log is long, paste it whole; I'd rather read the real text than a summary of it.
