I need to see what's failing. A few things to start:

- What's the error output from the failed deploy?
- What's the deploy setup (GitHub Actions, Vercel, Docker to a cluster, something else)?
- The working directory `/tmp/tmp.Lc7UrJXM36` isn't a git repo and looks empty. Where does your project actually live?

If you point me at the repo path and paste the failure log, I'll dig in.
