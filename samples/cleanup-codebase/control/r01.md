I can't see anything to work from yet: the working directory `/tmp/tmp.BYHy31CNlI` holds only an empty `.claude/settings.json`, it isn't a git repository, and there's no project, CI config, or deploy script here. So I have no way to guess at the cause from what's in front of me.

The fastest path is for you to point me at the real thing. Most useful, roughly in order:

- The failing deploy's error output, or the last 50 or so lines of the log.
- The path to the actual project directory, if it's elsewhere on this machine.
- What runs the deploy (GitHub Actions, a Makefile, a shell script, a platform like Vercel or Fly) and the config file for it.

If nothing about your own code changed yesterday, the usual culprits are something moving underneath you: a floating dependency or base image tag that pulled a new version, an expired token or rotated credential, or a change on the deploy platform's side. The log will normally say which of those it is in the first error line, so start there.
