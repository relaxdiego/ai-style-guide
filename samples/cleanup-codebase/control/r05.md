I can't see the project from here: the working directory `/tmp/tmp.BYHy31CNlI` is empty apart from an empty `.claude/settings.json`, and it isn't a git repository. So there's no build config, no CI definition and no history for me to compare against yesterday's working state.

Point me at the actual project directory and I can start narrowing it down. The things that will move fastest:

- The path to the repo, or the command you run to deploy.
- The failure output, ideally the whole log rather than the last line, since the real cause is often a few steps above where it stops.
- Whether the last change was on your side (a commit, a dependency bump, a config edit) or nothing changed locally and it simply stopped working, which points at an expired credential, a rotated token, a registry or base image that moved, or a change in the deploy target.

That last distinction is usually the fork in the road. If nothing on your side changed between the last good deploy and yesterday, the cause is almost always external and time-based, and a certificate or token expiry is the first thing worth checking.
