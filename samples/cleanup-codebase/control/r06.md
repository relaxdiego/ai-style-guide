There's no code here to clean up — the working directory `/tmp/tmp.yU17JlR7pQ` is empty, and so are the sibling temp directories alongside it. The only file anywhere under `/tmp` is the `CLAUDE.md` that carries your writing conventions, so nothing has been checked out or copied into this workspace yet. Point me at the path where the codebase actually lives and I'll start there; if you expected it to be in this directory, something upstream of this session probably failed to populate it, and it's worth confirming that before I touch anything.

Once I can see the code, tell me what "clean up" means for you, because the useful reading varies a lot: dead code and unused dependencies, formatting and lint conformance, restructuring modules, tightening types, or improving test coverage are quite different jobs. If you have no strong preference, I'll read through it, report what I find worth changing, and let you pick from that before I edit.
