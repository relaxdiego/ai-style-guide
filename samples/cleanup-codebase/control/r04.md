I can't get started on this one yet: the working directory `/tmp/tmp.BYHy31CNlI` is empty apart from an empty `.claude/settings.json`, and it isn't a git repository, so there's no deploy configuration, pipeline definition, or history here for me to read.

To make progress I need you to point me at the actual project, or bring the evidence to me. The most useful things, roughly in order: the path to the repository if it's elsewhere on this machine, the failing deploy's log output (the error and the surrounding lines, not just the final "failed" line), and what changed around the time it broke. That last one matters most — a deploy that worked and then didn't usually means either a commit landed, a dependency or base image floated to a new version, or something outside the repo moved, such as an expired credential, a rotated token, a filled disk, or a registry or provider change. If nothing was committed, that third category becomes the first place I'd look.

Tell me where the project lives and paste the log, and I'll take it from there.
