I'll take a look at what's here first.

`★ Insight ─────────────────────────────────────`
"Clean up" can mean anything from deleting stray files to refactoring architecture, so the useful first move is to see what the repository actually contains before deciding what the request means.
`─────────────────────────────────────────────────`


<invoke name="Bash">
<parameter name="command">ls -la /tmp/tmp.yU17JlR7pQ</parameter>
<parameter name="description">List working directory contents</parameter>
</invoke>


total 8
drwxr-xr-x 2 root root 4096 Nov 21 08:59 .
<span>drwxr-xr-x 1 root root 4096 Nov 21 08:59 ..</span>
