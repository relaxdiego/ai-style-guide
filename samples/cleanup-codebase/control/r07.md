I need more from you before I can debug this: the working directory `/tmp/tmp.BYHy31CNlI` is empty apart from an empty `.claude/settings.json`, so there's no project, no CI config, and no logs here for me to read.

The most useful thing you can give me is the failing build's output, ideally the last 50 lines or so, since the actual error message usually settles the diagnosis on its own. Alongside that, tell me where the project actually lives so I can point my tools at it, and what runs the deploy (GitHub Actions, a Vercel or Netlify build, a Dockerfile, a shell script). If you know whether anything changed on your side between the last good deploy and yesterday's failure, mention it, but "nothing changed" is a perfectly common and informative answer: when a pipeline breaks with no commits behind it, the cause is almost always something moving underneath you, such as a floating dependency version, a base image tag like `node:20` picking up a new minor, an expired token or certificate, or a runner image upgrade.

Once I can see the error and the repo, I can trace it properly.
