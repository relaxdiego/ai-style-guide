The working directory `/tmp/tmp.BYHy31CNlI` is empty apart from an empty `.claude/settings.json`, so there's no project here for me to look at and it isn't a git repository either. I can't see your deploy config, CI files, or history from where I'm running.

To get moving, the most useful thing you can give me is the path to the actual project checkout, plus the deploy failure output itself — the log from the failing run, or at least the error and the step it dies on. If you also know roughly what changed around yesterday (a merge, a dependency bump, a base-image or runner update, an expiring credential or certificate), say so, since a deploy that worked and then stopped without a code change usually points at something outside the repo.
