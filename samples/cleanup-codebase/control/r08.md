There's nothing here for me to diagnose from: `/tmp/tmp.BYHy31CNlI` contains only an empty `.claude/settings.json`, no project code, no config, and it isn't a git repository. So whatever is failing, I'm not looking at it yet.

The fastest way forward is either to point me at the actual project directory, or to paste the failure output. The error text plus the surrounding log lines usually settles it on its own, and a "started failing yesterday with no code change on my side" pattern almost always means something moved underneath the build rather than inside it: a floating dependency version, a base image tag that got rebuilt, an expired credential or certificate, or a change in the CI runner image.

Things that would let me narrow it quickly:

- The deploy log, from the first error through the end.
- Where it runs (GitHub Actions, GitLab CI, Vercel, a Fly/Heroku push, a shell script) and roughly what it deploys.
- Whether the last successful run and the first failing one were the same commit, or whether anything landed in between.
- Whether it fails at build, at the push/upload, or after deploy in a health check.

If the repository is somewhere else on this machine, give me the path and I'll read the CI config and lockfiles directly rather than working from description.
