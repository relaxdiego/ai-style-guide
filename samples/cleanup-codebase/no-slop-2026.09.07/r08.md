The working directory is empty (only `.claude/` config), so there is nothing here for me to inspect. I need you to point me at the project and the failure.

Fastest path: paste the failing deploy log, or tell me the path to the repo and I will read it. Also useful, in rough order of how often it explains a deploy that broke overnight without a code change:

- **Log output** of the failed step, from the first error, not just the last line.
- **What changed** between the last green deploy and the first red one: commit, config, secret rotation, or nothing at all.
- **Where it deploys**: CI provider and target (GitHub Actions, GitLab, Vercel, ECS, k8s, something else).
- **Dependency pinning**: whether the build resolves versions at deploy time (`latest` tags, unpinned lockfile, floating base image), which is how a build with no commits behind it starts failing.
- **Credential expiry**: token, cert, or cloud key with a lifetime that ended yesterday.
