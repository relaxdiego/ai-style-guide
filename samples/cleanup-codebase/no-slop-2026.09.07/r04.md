The working directory `/tmp/tmp.6QHyt2P5wA` is empty except for Claude config files, so there's no project, no CI config, and no logs for me to read. I can't diagnose anything from here.

Point me at the real thing:

- **Repo path** on this machine, or `cd` me to it.
- **Deploy platform**: GitHub Actions, Vercel, Fly, ECS, Heroku, something else.
- **Failure output**: paste the error, or give me the path to a log file. The first error line matters more than the final "deploy failed" line.
- **What changed**: last green deploy vs first red one. A commit range, a dependency bump, a rotated credential, or nothing on your side at all.

If nothing in your repo changed between the last success and the failure, the usual suspects are an expired token or secret, a floating dependency version that moved, or a base image tag that got republished.
