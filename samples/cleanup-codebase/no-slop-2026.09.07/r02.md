`/tmp/tmp.6QHyt2P5wA` is empty apart from `.claude/`, and it is not a git repo, so there is nothing here for me to read. Either point me at the actual project directory or paste the details below.

What I need to make progress:

- **Failure output.** The deploy log from the failed run, ideally the last 50 lines plus the first error, not just the final "deploy failed" line.
- **Where it runs.** GitHub Actions, GitLab CI, Vercel, Fly, ECS, Kubernetes, a shell script on a box.
- **Last good run.** Timestamp or commit SHA of the last deploy that worked.
- **What changed between them.** `git log --oneline <last-good>..HEAD`, plus any changes to the CI config, base image tag, or secrets.
- **Whether anything changed outside your repo.** Expired token or certificate, a rotated credential, a `:latest` base image that moved, a registry or cloud provider incident, a dependency published a new version and your lockfile does not pin it.

"Started failing yesterday with no code change" usually lands on the last item. An expired token and a floating tag are the two most common, and both look identical from the app side: the build or push step fails on something you did not touch.
