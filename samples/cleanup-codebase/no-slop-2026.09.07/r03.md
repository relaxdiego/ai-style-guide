I can't see anything to diagnose: `/tmp/tmp.6QHyt2P5wA` holds only `.claude/output-styles/no-slop.md` and `.claude/settings.json`, and it isn't a git repo. No project code, no CI config, no logs.

Point me at the real checkout, or paste these:

| What | Why it usually explains a same-day break |
| --- | --- |
| The failing log, from first error line to the end | The actual cause is often 50 lines above the final "deploy failed" |
| Deploy target and tool (GitHub Actions, Vercel, Fly, k8s, Ansible) | Determines where the config and the moving parts live |
| Whether the last green run and the first red run differ in code | Separates "your commit" from "the world changed under you" |
| Lockfile present and committed? | Unpinned transitive deps break builds overnight with no commit of yours |
| Any secret, token, or cert with a rotation date near yesterday | Expiry is the single most common "worked Friday, broke Monday" |

If the same commit that passed two days ago fails now, the cause is outside your repo: an expired credential, a base image or action tag that moved, a registry or package version pulled, or a runner image upgrade. Rerunning the last known-good deploy unchanged is the fastest way to confirm which side of that line you're on.
