I can't help yet: `/tmp/tmp.6QHyt2P5wA` contains only your Claude config, no project, and it isn't a git repo. Nothing to diagnose from here.

Send me:

- The failing output. Full log from the first error, not the summary line.
- Where it runs: GitHub Actions, GitLab CI, Vercel, Fly, a script on a box.
- The path to the actual repo, or `cd` me into it.
- What changed since the last green run: your commits, a merged PR, or nothing at all.

That last one splits the search. If your code didn't change, the cause is outside your repo, and the overnight breakers are almost always one of these: a floating dependency tag that published a new version, an expired token or certificate, a CI runner image bump, a base image `:latest` moving, or cloud credentials rotated by someone else. If your code did change, `git log` since the last successful deploy is the whole suspect list.
