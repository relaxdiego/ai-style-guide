Working directory `/tmp/tmp.6QHyt2P5wA` is empty except for `.claude/`, so there's nothing here for me to inspect. I need pointers before I can do anything useful.

- Path to the repo or project directory.
- The failure output: CI log, `docker build` error, deploy tool output. Paste the tail, including the first error line rather than only the final summary.
- What deploys it: GitHub Actions, GitLab CI, Vercel, Fly, ECS, a shell script.
- Whether the last successful deploy and the first failing one differ by a commit, or whether the same commit now fails. A failing rebuild of unchanged code usually means a moving dependency, a base image tag that shifted, or an expired credential.
