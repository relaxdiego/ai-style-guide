I can't see your project from here. The working directory `/tmp/tmp.6QHyt2P5wA` is empty except for Claude config, and it isn't a git repo, so there's no code, no CI config, and no logs to read.

Point me at it and I'll dig in. Most useful, in order:

- Path to the repo, or `cd` me into it.
- The failing deploy log, or the last ~50 lines of it. Paste it or give me a file path.
- What the deploy is (GitHub Actions, Vercel, Fly, k8s, shell script) and the command that fails.
- Whether anything landed yesterday: a merge, a dependency bump, a rotated secret, an expired cert, a base image retag.

For a deploy that worked Friday and fails Monday with no code change, the usual causes are an unpinned dependency or base image that moved, an expired credential or certificate, a runner/CLI version bump on the provider side, or a quota/permission change on the target account. The log usually names which one in the first error line.
