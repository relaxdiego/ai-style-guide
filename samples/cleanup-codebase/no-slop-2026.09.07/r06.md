The working directory `/tmp/tmp.6QHyt2P5wA` is empty except for `.claude/`, so there is nothing here for me to inspect. I need you to point me at the actual project or paste the evidence.

Fastest path, pick whichever you have:

| What to give me | Why it helps |
|---|---|
| Path to the repo | I can read the deploy config and recent commits myself |
| The failing job's log, especially the first error, not the last | The last line is usually a summary; the first is the cause |
| What changed since the last green run | Commits, a dependency bump, a base image tag, an expired credential, a runner image update |
| Deploy target and tool | Kubernetes, ECS, Vercel, Fly, Terraform, plain SSH, each fails differently |

The "started failing yesterday with no code change" pattern is usually one of: a rotated or expired secret, a floating tag (`:latest`, `node:22`, an unpinned action) that moved under you, a registry or provider rate limit, or a CI runner image upgrade. The log tells us which in one line.
