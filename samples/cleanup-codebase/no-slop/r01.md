I need to see the deploy before I can tell you anything useful. Right now I have a working directory at `/tmp/tmp.q3pZrhCQiS` that isn't a git repo, so I can't look at recent commits or CI config.

Point me at any of these and I'll dig in:

- The failing build/deploy log (paste it, or give me the file path)
- The repo path, if the project lives somewhere other than `/tmp/tmp.q3pZrhCQiS`
- What the deploy target is (GitHub Actions, Vercel, Fly, k8s, a shell script)

If the log is long, paste the last 50 lines and the first error line — those two usually settle it.
