`/tmp/tmp.q3pZrhCQiS` is empty — only `.claude/output-styles/no-slop.md` and `.claude/settings.json`, and it's not a git repo. There's no project here for me to inspect, so I can't diagnose anything yet.

To get moving, give me whichever of these you have:

- **The failing output** — full log from the deploy, especially the first error, not the last. Paste it or point me at a file path.
- **Where the project actually lives** — a path I can `cd` into, or a repo to clone.
- **What "deploy" means here** — CI provider (GitHub Actions, GitLab, Jenkins), target (Vercel, Fly, k8s, bare VM), and the command that runs.
- **What changed** — last green deploy vs. first red one. Commits, a dependency bump, a rotated credential, a base image tag that moved.

If nothing on your side changed, the usual suspects are an unpinned dependency or base image that floated to a new version, an expired token or certificate, and a provider-side deprecation that hit its cutoff date. The log will tell us which in about ten seconds.
