I need more to go on. Right now I don't know what your deploy is, what it does, or what "failing" looks like.

Give me any of these and I can start:
- The error output or failing log lines
- The deploy target and tooling (GitHub Actions, Vercel, Docker/K8s, Terraform, a shell script)
- Whether the working directory `/tmp/tmp.q3pZrhCQiS` is the project — it's currently empty and not a git repo
- What changed around yesterday: a merge, a dependency bump, a credential rotation, an upstream base image

If the project lives somewhere else on this machine, point me at the path and I'll read the config and logs myself.
