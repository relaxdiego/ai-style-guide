The defects live between the parts and in things that aren't there, and review reads one hunk at a time. That's why you can't point at a line.

What's actually different:

- **Every line reads as equally deliberate.** In code a person wrote, you can feel where they hesitated: an odd cast with a comment, a weirdly specific constant, an early return that clearly cost an afternoon. That tells you which parts are safe to change. I produce prose of uniform confidence, so you have to re-derive every decision yourself before touching anything.
- **Locally correct, globally redundant.** I make each function self-sufficient: re-validate the argument, re-check the null that can't happen, re-normalize the path. Each one passes review alone. Six months later an invariant changes and it's enforced in five places that now disagree.
- **I write a new helper instead of finding yours.** I don't hold your codebase in mind the way you do, so I reach for a fresh `format_user_line` rather than the one in `util/render.py` that does 90% of it. The new one is fine on its own merits. Now there are two, and the next person picks whichever they grep first.
- **Defensive error handling in place of decisions.** A `try/except` with a fallback default looks like care during review. It's me declining to decide what should happen on failure. The bug surfaces three layers up as a wrong value with no stack trace.
- **Completeness where you asked for one case.** You wanted the read path; I filled in write, delete, and the reverse conversion because the set looked incomplete. Those paths have no callers and no tests exercising them for real, and nobody deletes them later because nobody can prove they're dead.
- **Comments that restate the code.** The reason behind a choice is exactly what I don't have. I wasn't in the argument that produced the requirement, so I write what the line does, which you could already read.
- **Loose shapes passed between layers.** Dicts and broad types adapt to whatever each call site needs, which is locally convenient and makes a field rename a search-and-pray operation.

What changes the output: tell me the invariant and where it's enforced, so I stop re-checking it everywhere. Point me at the existing helpers before I write one. Ask me to mark the lines I was unsure about, and to state what I considered and rejected, in the PR description rather than in comments. And say "only the read path" out loud, because otherwise I'll finish the set.
