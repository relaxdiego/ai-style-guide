Every defect in it is an absence, so there's no line to point at. The code contains what you asked for; what's missing is everything a person would have left behind while writing it.

- **Decisions get spread across call sites instead of recorded in one.** When a real constraint appears (this list can be empty, this timestamp is UTC, this ID is only unique per tenant), I handle it correctly everywhere it comes up. Each site reads fine. But there's no function or type you can read to learn the rule, so the next person has to rediscover it from scratch before they can safely change anything, and they won't know when they've found all the places.
- **Defensive code I can't justify.** I add a null check, a `try/except`, a fallback default because it might be needed. You can't tell those apart from the guards covering a failure that actually happened, so you can't delete any of them, and they accumulate. Ask me "what input breaks if I remove this?" If I can't name one, it should go.
- **Tests that assert the implementation rather than the requirement.** They mirror the code I just wrote, pass forever, and catch nothing. Worse than absent tests, because the suite reports coverage it isn't providing.
- **No trace of the alternatives.** Code written by someone maintaining a system is asymmetric: the odd branch is there because the obvious approach broke in staging. Mine is uniform, so you can't tell which parts I reasoned about and which I produced on autopilot, and you end up re-deriving all of it at equal cost.
- **Written to the prompt, not to the system.** Your requirement has a direction it's been moving for two years. The prompt is one frame of that. The result fits the sentence you typed with no slack in the direction the requirement is actually travelling.

What changes the outcome is making the invariants and the rejected options land somewhere durable: a comment at the point of decision, a named type, a PR body that says what I considered and dropped. Ask me for those explicitly, because review of a diff can't surface them and I won't volunteer them.
