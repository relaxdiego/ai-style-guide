The code has no history, and maintenance is mostly reading history. A person's code accretes: each guard, each odd branch, each weird default got there because something broke, and the shape of the file records which parts are dangerous. I write the whole thing at once from your description, so every line has the same provenance and the same apparent confidence. You can't point at the wrong line because the problem is a missing signal spread across all of them.

Concrete ways that shows up:

- **Flat emphasis.** The subtle concurrency bit and the argument-parsing boilerplate get identical comment density, naming care, and structure. Nothing tells you where to be careful.
- **Symmetry that isn't real.** Three handlers written to look parallel when one has a different failure mode. The uniformity is mine, not the domain's.
- **Invented constants presented as decisions.** Timeout of 30s, batch size 100, three retries. Plausible, unmeasured, unmarked. You cannot tell which numbers came from your system and which I made up.
- **Local error handling with no global error model.** Every try/except is individually defensible at review; together they mean no failure ever reaches anyone who could act on it.
- **Abstractions sized to the prompt.** I generalize over the two cases you described, not the five in your codebase, so the helper fits your example and fights the third caller.
- **Defensive code for impossible states.** A `None` check on something that can't be `None` teaches the next reader that it can be, and now nobody can delete it.
- **Comments that restate the code instead of recording the rejected alternative.** The "why" is gone, so every line looks equally load-bearing and nothing is safe to remove.

Review doesn't catch this because review asks "is this line correct and does the diff do what it says," and each line passes. What's missing is information that a diff never displays. If you want it fixed, make me produce it explicitly: ask for a short decision log with the alternatives I rejected, require that every invented constant be labeled as a guess with the input needed to replace it, and tell me not to write an abstraction until there are three real callers. Those three constraints remove most of it.
