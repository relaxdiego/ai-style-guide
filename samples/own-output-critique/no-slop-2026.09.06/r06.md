No single line is wrong, which is why you can't find one. The defects are distributional: they live in the ratios between parts, the absence of history, and the flat texture across the file.

Specifically, what my code lacks that yours has:

- **No effort gradient.** You write the hard part carefully and the boilerplate carelessly. That unevenness is a map: dense code means "I thought about this," sloppy code means "nobody cared." My output has uniform polish, so nothing tells a maintainer where the dragons are. Every part looks equally considered, which means none of it does.
- **No record of rejected alternatives.** Human code carries scars — a strange ordering, a check with `# do not remove, breaks on empty batch`. Mine is written as if the repo started this morning. Later you can't tell which decisions are constraints and which are coin flips, so you touch nothing.
- **Defensive code for impossible states.** Null checks, try/except, input validation on paths that can't be reached. Reviewers approve it because caution reads as responsible. Six months on, you can't delete a branch without proving the condition is unreachable, and there are forty of them.
- **Abstraction sized for callers that never arrive.** A helper with one call site, a config dict with one key, a strategy interface with one implementation. Clean in review, pure overhead in maintenance.
- **Invariants asserted in names and docstrings but not enforced.** `validated_payload` that nothing validated. The name becomes a fact the next person relies on.
- **Tests that mirror the implementation, not the requirement.** They go green and stay green. When you change behaviour, you rewrite the test to match, so the suite never once says no.
- **One concept, several vocabularies.** `user_id`, `uid`, `account_key` for the same value, because each file was written in a separate context with no memory of the last.
- **Nothing declined.** You leave features out from taste. I take every suggestion in the prompt, so files accrete instead of holding a position.

What actually fixes it, in order of payoff: make me state the reason for each non-obvious choice in the diff description rather than in comments, then delete the ones with no reason; ban helpers with a single caller; ban error handling for conditions I can't name a trigger for; and give me the history — the migration, the outage, the client that sends malformed dates — since I will otherwise write code for a system with no past.
