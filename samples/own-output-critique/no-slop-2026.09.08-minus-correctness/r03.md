The code is locally correct and globally arbitrary. Each function is written against its own spec, not against the accumulated decisions in your repo, and that difference doesn't show up on any single line.

- Every choice that could have gone two ways went whichever way was fine in that file. There's no rule you can infer from one module and carry to the next, so maintaining it means reading all of it, every time, instead of learning the codebase once.
- Nothing marks which decisions were deliberate. A person who picks a retry count of 3 has rejected 1 and 10, and the reason usually survives somewhere: a name, a comment, an awkward signature they chose on purpose. Mine looks equally intentional everywhere, so you can't tell what's safe to change and you end up treating all of it as fragile.
- Defensiveness that passes review one item at a time: a parameter with a default nobody passes, an interface with one implementer, error handling for a branch that can't happen. Each is justifiable. Together they hide which paths actually execute.
- I match your formatting well enough to clear lint and review while duplicating concepts underneath. Two slightly different notions of "order" with identical style read as one system until you change one of them.
- Producing code costs me nothing, so I don't compress. A human deletes the third helper rather than maintain it; I keep it because it was free to write.

What helps: tell me the existing convention instead of letting me infer it, ask what I rejected and why before you merge, and delete anything I added that no current caller exercises.
